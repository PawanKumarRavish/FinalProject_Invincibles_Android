<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <!-- <item name="android:fontFamily">@font/helvetica_condensed_bold</item>-->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="app_form_style">
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
    </style>

    <style name="TabLayoutStyle" parent="Base.Widget.Design.TabLayout">
        <item name="android:textSize">10sp</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <style name="MyPopupMenuStyle" parent="@style/Widget.AppCompat.Light.PopupMenu.Overflow">
        <item name="android:textColor">#4b4a4a</item>
        <item name="android:layout_marginLeft">5dp</item>
        <item name="android:layout_marginRight">5dp</item>
        <item name="android:layout_marginTop">2dp</item>
        <item name="android:layout_marginBottom">2dp</item>
        <item name="android:dropDownListViewStyle">@style/PopupMenu.MyAppTheme</item>
        <!--<item name="popupMenuStyle">@style/PopupMenuStyle</item>-->
        <item name="android:listPreferredItemHeightSmall">40dp</item>
    </style>

    <style name="PopupMenu.MyAppTheme" parent="Widget.AppCompat.ListView.DropDown">
        <item name="android:divider">#eee</item>
        <item name="android:dividerHeight">1dp</item>
        <item name="android:background">@drawable/custom_layout</item>

    </style>

</resources>
